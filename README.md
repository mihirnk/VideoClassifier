Backend:

set up a virtual environment and install the packages in requirements.txt.
to run, go into the CoCrChallenge folder and execute "python3 api.py"

then to run the frontend, install any necessary front end packages with npm install
then go into the frontend folder and run "npm run dev"
the localhost and port will become available to you to access the application.
